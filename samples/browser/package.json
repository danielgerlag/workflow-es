{
  "description": "Workflow ES samples for the browser",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "serve"
  },
  "devDependencies": {
    "serve": "^1.4.0",
    "typescript": "^2.2.1"
  },
  "dependencies": {    
    "jquery": "^3.1.1",
    "systemjs": "^0.19.41",
    "workflow-es": "^1.0.3"
  }
}
